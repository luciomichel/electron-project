{
  "name": "electron-project",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^22.0.0",
    "electron-builder": "^20.28.3"
  },
  "dependencies": {
    "electron-updater": "^3.1.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/luciomichel/electron-project.git"
  },
  "publish": [
    {
      "provider": "github",
      "owner": "luciomichel",
      "repo": "electron-project",
      "private": false
    }
  ]
}
